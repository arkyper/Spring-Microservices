server:
  port: 8081;
  

spring:
  datasource:
    url: jdbc:postgresql://localhost:3306/micro
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true